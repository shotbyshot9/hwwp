# hwwp — Homeground of Writer Word Processor

작가를 위한 워드프로세서. [rhwp](https://github.com/edwardkim/rhwp)(MIT)를 갈라 만들었다.

이름은 한글과컴퓨터의 **hwp** 를 패러디했고, `http` 처럼 읽어 "에이치 따따피" 다.
드라이브 작업 폴더도 `hwwp` 다. 개명 전 `WHP` 폴더가 있으면 새로 만들지 않고
이름만 바꿔 이어 쓴다 — 그래야 그동안 저장한 문서가 그대로 보인다.

풀이하면 **H**omeground **o**f **W**riter **W**ord **P**rocessor 다. 문서 엔진(HWP/HWPX 파싱·렌더링·편집)은 rhwp 그대로 쓰고, 그 위에
**작가를 위한 배명훈 모드**를 얹는다.

만든 사람: **류지원**

## 출신

- 문서 엔진: [rhwp](https://github.com/edwardkim/rhwp) — MIT, © 2025-2026 Edward Kim
- 배명훈 모드의 착상: [Writer's Homeground](https://writerhomeground.lovable.app/) —
  배명훈 작가의 소설에 나오는 워드프로세서에서 출발한 웹 실험작. 문장부호를 하나 찍을
  때마다 박수와 폭죽이 터지는 "응원하는 워드프로세서"라는 발상을 hwwp 로 옮겼다.

rhwp 는 MIT 라이선스이므로 파생·개명·재배포가 자유롭다. 원저작권 고지는 `LICENSE`와
제품 정보 대화상자에 그대로 남긴다.

## 저작권

- hwwp (이 파생물의 변경분) — © 2026 류지원
- rhwp (문서 엔진 원본) — © 2025-2026 Edward Kim, MIT

두 고지 모두 `LICENSE` 와 제품 정보 대화상자에 함께 표시한다. MIT 는 원저작권 고지를
사본에 남길 것을 요구하므로 rhwp 줄은 지울 수 없고, 그와 별개로 hwwp 의 변경분에 대한
저작권은 류지원 에게 있다.

### 내부 이름은 rhwp 로 둔다

디렉터리(`rhwp-studio/`), 크레이트(`rhwp`), wasm 산출물(`pkg/rhwp.js`), localStorage
키(`rhwp-settings`)는 원본 이름 그대로다. **hwwp 는 제품이고 rhwp 는 그 엔진이다** —
이름이 갈리는 것이 어설픈 게 아니라 정확하다.

바꾸지 않는 이유는 업스트림이다. hwwp 의 변경은 새 파일 53개와 원본 수정 22개로
나뉘는데, 배명훈 모드·드라이브·제목 줄이 전부 새 파일이라 애초에 충돌하지 않는다.
이름을 바꾸면 이 구조가 무너진다 — 크레이트를 바꾸면 `src/` 의 rhwp 참조 1,202건이
전부 달라져 업스트림의 Rust 커밋마다 충돌하고, 디렉터리만 바꿔도 git 의 디렉터리
rename 추적에 기대게 되어 업스트림이 새로 넣는 파일마다 자리를 손으로 정해야 한다.

`mydocs/` 의 rhwp 언급 63,964건은 **rhwp 의 개발 기록**이다(PR 아카이브·작업 계획·
리포트). 거기서 이름을 바꾸면 rhwp 에서 있었던 일을 적은 문서가 사실과 달라진다.

사용자에게 보이는 곳에는 rhwp 가 없다 — 제목, PWA manifest, 제품 정보, 드라이브 폴더
이름 모두 hwwp 다. 스튜디오 소스에 남은 180건은 주석·import·개발자 콘솔(`rhwpDev`)·
`console.log` 뿐이고 UI 문구에는 0건이다.

### 원격 저장소

`upstream` 이 원본(`edwardkim/rhwp`)이다. 본인 저장소를 `origin` 으로 따로 두고,
업스트림 변경은 이렇게 가져온다.

```bash
git fetch upstream
git merge upstream/main
```

## 구글 드라이브 저장 (작업 중)

rhwp 는 저장할 때마다 다운로드 폴더에 새 파일이 떨어진다. hwwp 는 드라이브의 `hwwp`
폴더에 자동 저장한다 — 저장 위치를 묻지 않고, 저장 상태만 제목 줄에 보여 준다.

| 파일 | 역할 |
| --- | --- |
| `storage/doc-name.ts` | 이름 규칙. `새 문서` → 중복이면 `새 문서(1)` |
| `storage/storage-backend.ts` | 로컬·드라이브 공통 경계 |
| `storage/drive-auth.ts` | 토큰. 만료 5분 전 무음 갱신 |
| `storage/drive-client.ts` | Drive REST v3 |
| `storage/drive-backend.ts` | 저장소 구현 + 이름 중복 회피 |
| `storage/autosave-controller.ts` | 2초 디바운스 / 30초 강제 / 백오프 재시도 |
| `ui/title-bar.ts` | 문서 이름·저장 상태·연결 버튼 |

범위는 `drive.file` 하나만 쓴다. 앱이 만든 파일에만 닿아 구글 보안 심사 대상이
아니고, hwwp 폴더와 그 안의 문서는 전부 앱이 만든 것이라 요구사항이 충족된다.

토큰 수명(1시간)은 구글이 고정한 값이라 늘릴 수 없다. 대신 응답의 실제 `expires_in`
을 쓰고 만료 5분 전에 미리 갈아 끼워, 쓰는 동안에는 만료를 느끼지 못하게 한다.

`파일 → 구글 드라이브에서 열기…` 로 hwwp 폴더의 문서를 불러온다. 바이트를 직접
로더에 넘기지 않고 기존 `open-document-bytes` 경로에 태운다 — 저장하지 않은 변경
확인, 암호 문서 처리, 오류 표시가 모두 그 경로에 있다. 로드가 끝난 뒤에 저장 대상을
그 파일로 붙여, 이후 자동 저장이 새 파일을 만들지 않고 같은 문서를 덮어쓴다.

대화상자 안의 「드라이브에서 찾아보기…」는 Google Picker 를 띄운다. `drive.file` 범위는
앱이 만든 파일에만 닿아서 밖에서 올린 hwp 는 목록에 안 잡히는데, 피커로 고른
파일은 그 한 건에 한해 접근이 열린다. 범위를 전체 `drive` 로 넓히지 않고도
(= 구글 보안 심사를 받지 않고도) 드라이브 아무 곳의 문서를 열 수 있다.

피커는 MIME 으로 거르지 않는다 — 드라이브가 hwp 를 `application/x-hwp`,
`application/haansofthwp`, `application/octet-stream` 등 제각각으로 저장해 필터를
걸면 멀쩡한 문서가 목록에서 사라진다. 고른 뒤 확장자로 판단한다.

아직 안 된 것 — 크롬 확장용 인증 어댑터.

## 화면 보정 — 배율 100% = 용지 실물 크기

브라우저는 화면의 물리적 크기를 알 수 없다. CSS 픽셀은 96dpi 를 가정한 논리
단위라, 141dpi 화면에서 A4(210mm)를 794 CSS px 로 그리면 실제로는 14cm 남짓으로
작게 나온다. 설치형 워드프로세서가 OS 가 알려 주는 화면 DPI 로 하는 보정을,
웹앱에서는 사용자가 한 번 재 주는 수밖에 없다.

`보기 → 배율 → 화면 보정…` 에서 막대를 신용카드 긴 변(85.6mm, ISO/IEC 7810 ID-1)에
맞추면 1mm 당 CSS px 이 정해진다. 그 값으로 배율 100% 가 종이와 1:1 이 된다.

사용자가 고르는 배율은 "용지 실물 대비" 이고, 렌더 배율은 거기에 보정값을 곱한
값이다. 상태 표시줄은 반대로 되돌려 보여 준다 — 보정 여부와 무관하게 100% 는
언제나 실물 크기를 뜻한다. 보정 전에는 곱이 1 이라 종전과 똑같이 동작한다.

## 배명훈 모드

`보기 → 배명훈 모드` 또는 **Alt+Shift+F**. 나가기는 **Esc**.

두 겹으로 되어 있다.

### 1. 선(禪) 화면

메뉴바·도구 상자·서식 도구 모음·눈금자·상태 표시줄을 모두 걷어내고, 그 자리에
Writer's Homeground 의 화면을 그대로 옮긴다.

- **머리글** — 호박색 깃펜 표식, `배명훈 모드`,
  `배명훈 〈홈, 어웨이〉의 환호하는 에디터에서 영감을 받았습니다`.
  오른쪽에 토글 셋(폭죽·테마·효과음), 배속 단추, 나가기.
- **바닥글** — 단어수·글자수·경과 시간. 세션 목표를 정했으면 진행바도 함께.
- **색** — 원본과 같은 값을 쓴다. 밝게 `#F8F6F2` / `#2E241F`, 어둡게 `#0C0A09` /
  `#F0ECE6`, 포인트는 호박색 `#F59F0A`.

**배명훈 모드는 자기만의 테마와 배율을 갖는다.** 들어가면 어둡게 130% 로 바뀌고,
설정 배율이 창보다 넓어지면 폭 맞춤에서 멈춘다 — 글을 읽으려고 가로로 스크롤하는
것이 글쓰기 화면에서 가장 방해되는 동작이기 때문이다.
나오면 일반 편집 화면의 테마와 배율이 그대로 돌아온다. 두 화면의 설정은 서로를
건드리지 않는다 — 일반 화면을 밝게 100% 로 쓰면서 배명훈 모드만 어둡게 130% 로 둘 수 있다.

- 배명훈 모드 테마·배율: `보기 → 배명훈 모드 설정`, 또는 머리글의 해/달 토글
- 일반 편집 화면 테마·배율: 기존 `보기 → 테마` / `보기 → 배율` 그대로

구현은 `applyTheme()` 을 쓴다 — `setThemeMode()` 와 달리 화면에만 적용하고 설정에
저장하지 않는다. 진입할 때 일반 화면의 배율·테마 모드를 기억해 두었다가 나갈 때 되돌린다.

원본과 다른 점 하나 — hwwp 는 진짜 HWP 문서를 다루므로 가운데는 빈 textarea 가 아니라
실제 편집 용지다. 종이는 남기되 주변 크롬만 갈아 끼웠다.

**어두운 편집 용지** — 렌더러는 용지를 늘 흰색으로 그린다(WYSIWYG 이라 당연하다).
어두운 화면에 흰 종이만 남으면 눈이 부시므로, 배명훈 모드 + 어두운 테마에서는 그려진
결과를 통째로 반전해 어두운 종이에 밝은 글씨로 뒤집는다.

`invert(0.93) hue-rotate(180deg)` — `invert(a)` 는 `c' = c(1-2a) + a` 이므로
`a=0.93` 에서 흰색은 `#121212`, 검정은 `#EDEDED` 이 된다. 원본의 `#0C0A09` /
`#F0ECE6` 에 가깝다. 순수 반전(`a=1`)이면 종이가 완전한 검정이라 너무 세다.
`hue-rotate` 는 색 글씨·형광펜의 색상을 제자리로 되돌린다. 캐럿과 조합 오버레이도
같이 뒤집히도록 `#scroll-content` 통째에 건다.

한계 — 문서에 넣은 **사진·그림도 함께 반전된다**. 이미지를 빼려면 렌더러가 용지·글자
색을 인자로 받아야 한다. 배명훈 모드를 나가면 원래대로 흰 종이로 돌아온다.

**앱은 배명훈 모드로 켜진다.** 이 제품의 핵심이라 기본 상태로 삼았다. 문서가 있어야
들어갈 수 있으므로, 켤 때 문서가 없으면 먼저 만든다 — 첫 실행이면 사용법 문서를,
그다음부터는 빈 문서를. 일반 편집으로 시작하고 싶으면
`보기 → 배명훈 모드 설정 → 켤 때 배명훈 모드로 시작` 을 끈다.

제목 줄에도 **배명훈 모드** 버튼을 박아 두었다 — 메뉴를 뒤지지 않고 바로 드나든다.

**타자기 스크롤** — 캐럿이 화면 위쪽 40% 지점에 머물도록 스크롤을 따라 밀어 준다.
줄이 바뀌어도 시선이 아래로 흘러내리지 않는다.

**바로 쓰기** — 배명훈 모드에 들어가면 캐럿을 놓고 편집 입력에 포커스를 준다. 빈 문서를
한 번 클릭해야 입력이 먹던 편집기 기본 동작을 여기서는 건너뛴다.

### 2. 응원 레이어

문장부호(`. ! ? , ; : 。！？，、…`)를 찍을 때마다:

- 화면 **좌·우 가장자리에서 색종이가 터진다** (250ms 스로틀)
- **환호와 박수 소리**가 난다 — Writer's Homeground 가 쓰던 음원 15개를 그대로
  가져왔다(`rhwp-studio/public/sounds/sfx-01~15.mp3`, 2.1MB). 직전에 난 소리는
  연달아 고르지 않는다. 음원을 못 불러오면 WebAudio 합성 박수로 물러난다.
- **스트릭**: 2초 이상 멈추면 처음부터 다시 쌓인다. 쉬지 않고 이어 쓸수록 소리가
  커진다 — `0.35 + min(글자수/100,1)×0.3 + min(연속횟수/10,1)×0.3`, 상한 0.9
- 문장부호 없이 **50자 이상 달리면** 큰 축포가 세 갈래로 터지고 상승 차임이 얹힌다
- 연속 응원이 **20회**를 넘으면 가끔 **음성으로 칭찬**한다 — "대단해!", "Great!",
  "すごい!" 등 10개 국어에서 무작위로. 최소 5초 간격.

#### 응원 배속

문장부호만으로는 드문드문하다. 한국 소설의 한 문장은 평균 30~40자이고 한 글자는
평균 두 타이므로, 문장부호 응원은 **60타에 한 번** 꼴이다. 그래서 정해진 글자수마다도
터지게 하는 배속을 두었다.

| 단계 | 응원 간격 | 세는 글자수 |
| --- | --- | --- |
| **x1**(기본) | 60타 | 30자 |
| x2 | 30타 | 15자 |
| x3 | 20타 | 10자 |
| x5 | 12타 | 6자 |
| x10 | 6타 | 3자 |
| MAX | 모든 타 | 1자 |

머리글의 배속 단추를 누르면 `x1 → x2 → x3 → x5 → x10 → MAX → x1` 로 돈다. 옆의
세 개는 켜고 끄는 토글이지만 이것은 단계 조절기라, 아이콘 대신 지금 단계를 글자로
찍고 테두리를 둘렀다. x1 을 넘으면 호박색 테두리, MAX 는 채운다.

편집 엔진이 알려주는 것은 "타" 가 아니라 확정된 "글자" 이므로 두 타를 한 글자로
환산해서 센다(`focus/cheer-rate.ts`). 어느 배속에서든 **문장부호 응원은 그대로
남는다** — 문장 끝 `또는` 배속 문턱, 둘 중 하나면 터진다.

잦아질수록 한 번의 소리는 줄인다(`√(글자수/30)`, 하한 0.45). 같은 크기로 열 배
자주 터지면 응원이 아니라 소음이다. 축포는 배속과 무관하게 **한 문장에 한 번**만
터진다 — 아니면 긴 문장 하나가 화면을 폭죽으로 덮는다.

**세션 목표**를 정해 두면(500·1,000·2,000·5,000자) 바닥글 진행바가 차오르고 달성
순간 축포가 터진다.

### 설정

`보기 → 배명훈 모드 설정`

| 항목 | 값 |
| --- | --- |
| 테마 | 어둡게(기본) / 밝게 — 일반 화면과 별개 |
| 배율 | 100 / **130**(기본) / 160 / 200% — 일반 화면과 별개. 창을 넘으면 폭 맞춤에서 멈춤 |
| 응원 강도 | 조용히 / 기본 / 축제 — 한 번의 응원이 얼마나 큰가 |
| 응원 배속 | x1(기본) / x2 / x3 / x5 / x10 / MAX — 얼마나 자주 터지는가. 머리글 단추로 돌린다 |
| 폭죽 효과 | 켜기·끄기 |
| 박수 효과음 | 켜기·끄기 |
| 음성 칭찬 | 켜기·끄기 |
| 타자기 스크롤 | 켜기·끄기 |
| 세션 목표 | 없음 / 500 / 1,000 / 2,000 / 5,000자 |
| 켤 때 배명훈 모드로 시작 | 켜기(기본)·끄기 |

설정은 기존 `userSettings`(localStorage)에 `focus` 절로 저장된다.
`prefers-reduced-motion: reduce` 환경에서는 폭죽을 자동으로 끈다.

## 구현 구조

배명훈 모드는 **편집 엔진을 건드리지 않는다**. 붙는 지점은 딱 하나, 확정 입력 이벤트다.

```
rhwp-studio/src/
  focus/
    focus-mode.ts      진입·이탈, 머리글·바닥글, 타자기 스크롤, 세션 통계
    cheer-engine.ts    문장부호·배속 감지 → 박수 합성·폭죽·음성 칭찬, 스트릭
    cheer-rate.ts      배속 계산 (타 ↔ 글자 환산, 순환, 소리 감쇠) — 순수 함수
    confetti.ts        전용 캔버스 폭죽 렌더러 (외부 의존성 없음)
  command/commands/
    focus.ts           focus:* 커맨드와 메뉴 상태 동기화
  styles/
    focus-mode.css     선 화면 CSS와 fm-* 요소
```

연결 지점:

| 파일 | 변경 |
| --- | --- |
| `engine/input-handler-text.ts` | 확정 삽입 4곳에서 `text-inserted` 이벤트 발행 |
| `command/shortcut-map.ts` | Alt+Shift+F → `focus:toggle` (한글 자판 `ㄹ` 포함) |
| `core/user-settings.ts` | `FocusSettings` 절 추가 |
| `index.html` | 보기 메뉴에 항목 추가 |
| `main.ts` | `focusCommands` 등록 |

`text-inserted` 는 **확정된** 텍스트만 싣는다. IME 조합 중간 상태는 오지 않고,
조합은 `compositionend` 에서 완성된 글자로 한 번만 통지된다. 한글 두벌식에서
문장부호는 조합을 끝내고 일반 입력 경로로 들어오므로 응원이 정확히 한 번 터진다.

## 아직 안 한 것

- **문단 포커스(현재 문단만 선명하게, 나머지는 흐리게)** — 본문이 canvas 로
  렌더링되므로 렌더러(`view/page-renderer.ts`, `canvaskit-renderer.ts`) 단에서
  문단 단위 알파를 넣어야 한다. DOM 오버레이로는 안 된다.
- **뽀모도로 타이머** — 현재 세션 목표는 글자수 기준만 있다.
- **배명훈 모드 중 대화상자 억제(큐잉)**
- **전면 개명** — 제품 이름·창 제목·제품 정보는 hwwp 로 바꿨지만, 크레이트 이름과
  `pkg/rhwp.js` 등 WASM 산출물 식별자는 아직 rhwp 다. 빌드 파이프라인 전체를 함께
  옮겨야 해서 별도 작업으로 분리했다.

## 빌드

rhwp 와 동일하다.

```bash
wasm-pack build --target web --out-dir pkg
```

```bash
cd rhwp-studio && npm install && npm run dev
```

## 배포

Cloudflare Workers 정적 자산으로 올린다. `main` 에 푸시하면 자동으로 다시 빌드된다.

| | |
| --- | --- |
| 주소 | https://hwwp.shotbyshot9.workers.dev |
| 저장소 | `origin` = `shotbyshot9/hwwp` (private) |
| 빌드 명령 | `cd rhwp-studio && npm ci && npm run build` |
| 출력 | `rhwp-studio/dist` (`wrangler.jsonc` 가 지정) |

서버가 없다. 문서는 사용자 본인의 구글 드라이브로만 가고 배포자의 서버를 거치지 않는다.

`pkg/` 의 wasm 산출물을 커밋해 두는 이유는 빌드 환경에 Rust 툴체인이 없어서다.
엔진을 새로 빌드했으면 `pkg/.gitignore` 를 비운 상태로 되돌리고 함께 커밋해야 한다.

### 구글 콘솔 설정

배포 주소를 바꾸면 세 곳을 같이 고쳐야 한다.

| 항목 | 값 | 형식 |
| --- | --- | --- |
| OAuth 클라이언트 → 승인된 JavaScript 원본 | `https://hwwp.shotbyshot9.workers.dev` | 경로·`/`·와일드카드 불가 |
| API 키 → 애플리케이션 제한사항 | **`없음`** | 아래 참조 |
| API 키 → API 제한사항 | Google Picker API + Google Drive API | 둘 다 필요 |

**API 키에 HTTP 리퍼러 제한을 걸면 피커가 죽는다**(`The API developer key is invalid`).
피커 창이 `docs.google.com` 의 iframe 이라 그 안에서 나가는 요청의 출처가 우리 도메인이
아니기 때문이다. 자세한 사정과 그래도 안전한 이유는 `storage/drive-config.ts` 에 적었다.

`API 제한사항` 에 Drive API 가 빠져 있어도 같은 오류가 난다 — 창을 띄우는 것까지가
Picker API 고 목록을 채우는 것은 Drive API 다.

### 아직 안 한 것

- 개인정보처리방침·서비스 약관 페이지. 구글 동의 화면이 "링크가 표시되지 않는다" 고 안내한다.
- 자체 도메인. `*.workers.dev` 는 Cloudflare 소유라 구글에 소유 증명을 할 수 없어서,
  동의 화면에 앱 이름(`hwwp`) 대신 도메인이 뜬다. 도메인을 붙이면 해결된다.
